const LogoIcon = ({ imageDimensions }) => {
  return (
    <img
      className="relative w-[284px] h-[81px] object-cover"
      alt=""
      src={imageDimensions}
    />
  );
};

export default LogoIcon;
